#include "main.h"
/**
 * _print_rev_recursion -wcadsts the aee
 * @s: string
 *
 */
void _print_rev_recursion(char *s)
{
	int i, n = 0;

	for {i = 0; i != '\0'; i++)
	{
		n++
	}
	for (i = n - 1; i >= 0 ; i--)
	{
		_putchar(s[i]);
	}
}
