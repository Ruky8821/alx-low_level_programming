#include "main.h"
/**
 * reverse_array - output array in reverse
 * @a: pointer to the array
 * @n: length of array
 */
void reverse_array(int *a, int n)
{
	int x, y;

	for (x = n; x >= 0; x--)
	{
		return (a[x]
	}
}
